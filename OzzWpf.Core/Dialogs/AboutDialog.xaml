<Window x:Class="OzzWpf.Core.Dialogs.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i18n="clr-namespace:OzzMarkdown.i18n;assembly=OzzMarkdown.i18n"
        xmlns:local="clr-namespace:OzzWpf.Core.Dialogs"
        mc:Ignorable="d"
        Title="{x:Static i18n:LocalizedStrings.About}"
        Height="320" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Application Icon -->
        <Image x:Name="AppIcon"
               Grid.Row="0" Grid.RowSpan="6" Grid.Column="0"
               Width="192"
               Height="192"
               HorizontalAlignment="Center" 
               Margin="0,0,0,10"/>

        <!-- Product Name -->
        <TextBlock Grid.Column="1" Grid.Row="1"
                   Text="{Binding Product}" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,5"/>

        <!-- Version -->
        <TextBlock Grid.Column="1" Grid.Row="2" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,5">
            <Run Text="{x:Static i18n:LocalizedStrings.Version}" FontWeight="SemiBold"/>
            <Run Text="{Binding Version, Mode=OneWay}"/>
        </TextBlock>

        <!-- Description -->
        <TextBlock Grid.Column="1" Grid.Row="3" 
                   Text="{Binding Description}" 
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   TextWrapping="Wrap"
                   Margin="0,0,0,5"/>

        <!-- Copyright -->
        <TextBlock Grid.Column="1" Grid.Row="4" 
                   Text="{Binding Copyright}" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>


        <!-- GitHub Link -->
        <TextBlock Grid.Column="1" Grid.Row="5" 
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top">
            <Hyperlink x:Name="RepoLink" NavigateUri="https://github.com/ozalpd/OzzMarkdown" 
                       RequestNavigate="Hyperlink_RequestNavigate">
                <Run Text="{x:Static i18n:LocalizedStrings.ViewOnGitHub}"/>
            </Hyperlink>
        </TextBlock>

        <!-- OK Button -->
        <Button Grid.ColumnSpan="2" Grid.Row="6" 
                Width="80" Height="28" Margin="0,8,0,0"
                Content="{x:Static i18n:LocalizedStrings.ButtonOK}" IsDefault="True"
                Click="OkButton_Click"
                HorizontalAlignment="Center"/>


    </Grid>
</Window>
